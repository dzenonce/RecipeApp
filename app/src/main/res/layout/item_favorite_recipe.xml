<?xml version="1.0" encoding="utf-8"?>
<androidx.cardview.widget.CardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@id/cvRecipeCard"
    style="@style/StyleCard"
    android:layout_width="match_parent"
    android:layout_height="wrap_content" >

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginBottom="@dimen/margin_small_minus_2"
        android:orientation="vertical">

        <ImageView
            android:id="@id/ivRecipeImage"
            android:layout_width="match_parent"
            android:layout_height="@dimen/size_image_100"
            android:scaleType="centerCrop"
            tools:ignore="ContentDescription"
            tools:src="@drawable/burger" />

        <TextView
            android:id="@id/tvRecipeName"
            style="@style/StyleCardHeaderText14"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            tools:text="Бургер с ветчиной и сыром" />

    </LinearLayout>

</androidx.cardview.widget.CardView>